# AI-RPC Framework - 开源框架战略设计

## 🎯 核心定位

### 一句话定位
> **"AI-Native RPC Framework"** — 第一个将AI深度融入RPC核心链路的开源框架

### 差异化竞争力

| 框架 | 定位 | 核心优势 |
|------|------|----------|
| **Dubbo** | 企业级服务治理 | 生态完整、阿里背书 |
| **gRPC** | 高性能跨语言 | Google背书、HTTP/2 |
| **Thrift** | 跨语言序列化 | Facebook背书 |
| **AI-RPC** | **AI-Native智能RPC** | **AI驱动的自适应治理** |

---

## 🧠 AI-Native 设计理念

### 传统RPC vs AI-RPC

```
┌─────────────────────────────────────────────────────────────────────┐
│                    传统RPC框架                                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   配置驱动：                                                         │
│   • 负载均衡：Round-Robin / Random / 静态权重                       │
│   • 熔断阈值：10次失败触发                                          │
│   • 超时时间：3000ms                                                │
│   • 重试次数：3次                                                   │
│                                                                      │
│   问题：                                                             │
│   ❌ 参数需要人工调优                                               │
│   ❌ 无法适应运行时变化                                             │
│   ❌ 不同服务需要不同配置                                           │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    AI-RPC 框架                                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   AI驱动：                                                           │
│   • 负载均衡：实时预测节点健康，动态权重                            │
│   • 熔断阈值：根据历史模式自动调整                                  │
│   • 超时时间：根据P99延迟自适应                                     │
│   • 重试策略：根据错误类型智能决策                                  │
│                                                                      │
│   优势：                                                             │
│   ✅ 零配置自适应                                                   │
│   ✅ 运行时持续优化                                                 │
│   ✅ 一套框架适配所有服务                                           │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ 框架架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                      AI-RPC FRAMEWORK                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    Application Layer                           │  │
│  │  @AiRpcService  @AiRpcReference  Spring Boot Starter           │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                               │                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    Governance Layer                            │  │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌──────────┐ │  │
│  │  │AI-LoadBalance│ │AI-CircuitBrk│ │AI-Timeout   │ │AI-Retry │ │  │
│  │  │             │ │             │ │             │ │          │ │  │
│  │  │ Predictive  │ │ Adaptive    │ │ Self-tuning │ │ Smart    │ │  │
│  │  │ Routing     │ │ Threshold   │ │ Deadline    │ │ Decision │ │  │
│  │  └─────────────┘ └─────────────┘ └─────────────┘ └──────────┘ │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                               │                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    AI Brain Layer                              │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  Metrics Collector → Feature Engineering → ML Models    │  │  │
│  │  │                                                          │  │  │
│  │  │  • Latency histogram    • Sliding window    • LightGBM  │  │  │
│  │  │  • Error rate           • Trend detection   • LSTM      │  │  │
│  │  │  • CPU/Memory           • Anomaly score     • Prophet   │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                               │                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    Transport Layer                             │  │
│  │  Netty │ Custom Protocol │ Protostuff │ Connection Pool        │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                               │                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    Registry Layer                              │  │
│  │  Nacos │ Zookeeper │ Consul │ etcd                             │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🧠 AI Brain 核心模块设计

### 1. AI-LoadBalancer (已实现，需增强)

**当前：** 单一延迟指标 + 指数衰减
**目标：** 多维指标 + 可插拔模型

```
┌─────────────────────────────────────────────────────────────────────┐
│                    AI-LOADBALANCER V2                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   Input Features:                                                    │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  • latency_p50, latency_p95, latency_p99                    │   │
│   │  • error_rate_1m, error_rate_5m                              │   │
│   │  • cpu_usage, memory_usage                                   │   │
│   │  • active_connections, queue_length                          │   │
│   │  • gc_pause_time, gc_frequency                               │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                               │                                      │
│                               ▼                                      │
│   Model Options (SPI扩展):                                          │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  • ExponentialDecay (轻量级，默认)                          │   │
│   │  • LightGBM (准确，需要训练)                                 │   │
│   │  • NeuralNetwork (复杂场景)                                  │   │
│   │  • Custom (用户自定义)                                       │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                               │                                      │
│                               ▼                                      │
│   Output:                                                            │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  health_scores = {node_a: 0.85, node_b: 0.62, node_c: 0.23} │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 2. AI-CircuitBreaker (新增)

**传统熔断：** 固定阈值（如10次失败触发）
**AI熔断：** 自适应阈值 + 预测性开启

```
┌─────────────────────────────────────────────────────────────────────┐
│                    AI-CIRCUITBREAKER                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   Traditional:                                                       │
│   if (failure_count > 10) { circuit.open(); }                       │
│                                                                      │
│   AI-Powered:                                                        │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  1. 计算历史基准 (baseline)                                  │   │
│   │     baseline_error_rate = avg(last_7_days)                   │   │
│   │                                                              │   │
│   │  2. 检测异常 (anomaly detection)                             │   │
│   │     if (current_error_rate > baseline * 3σ) { alert }        │   │
│   │                                                              │   │
│   │  3. 预测性开启 (predictive)                                  │   │
│   │     if (error_trend.slope > threshold) {                     │   │
│   │         // 错误率在上升，提前熔断                            │   │
│   │         circuit.open();                                      │   │
│   │     }                                                        │   │
│   │                                                              │   │
│   │  4. 自适应恢复                                               │   │
│   │     半开状态放入少量流量，根据成功率决定完全恢复或重新熔断    │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 3. AI-Timeout (新增)

**传统超时：** 固定配置（如3000ms）
**AI超时：** 基于P99延迟自适应

```
┌─────────────────────────────────────────────────────────────────────┐
│                    AI-TIMEOUT                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   Traditional:                                                       │
│   timeout = config.get("timeout", 3000);                            │
│                                                                      │
│   AI-Powered:                                                        │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │                                                              │   │
│   │  // 收集历史延迟数据                                         │   │
│   │  latency_histogram = collect(last_5_minutes);                │   │
│   │                                                              │   │
│   │  // 计算P99 + 安全边际                                       │   │
│   │  p99 = latency_histogram.percentile(99);                     │   │
│   │  adaptive_timeout = p99 * 1.5;                               │   │
│   │                                                              │   │
│   │  // 防止极端值                                               │   │
│   │  timeout = clamp(adaptive_timeout, min=100ms, max=30s);     │   │
│   │                                                              │   │
│   │  优势：                                                      │   │
│   │  • 快服务用短超时，避免资源浪费                              │   │
│   │  • 慢服务自动延长，避免误杀                                  │   │
│   │  • 无需人工调优                                              │   │
│   │                                                              │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 4. AI-Retry (新增)

**传统重试：** 固定次数 + 所有错误都重试
**AI重试：** 智能决策是否值得重试

```
┌─────────────────────────────────────────────────────────────────────┐
│                    AI-RETRY                                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   Error Classification:                                              │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │                                                              │   │
│   │  Retryable:                                                  │   │
│   │  • TIMEOUT (可能是网络抖动)                                  │   │
│   │  • CONNECTION_REFUSED (节点重启中)                           │   │
│   │  • SERVICE_UNAVAILABLE (临时过载)                            │   │
│   │                                                              │   │
│   │  Non-Retryable:                                              │   │
│   │  • BUSINESS_ERROR (逻辑错误，重试无意义)                     │   │
│   │  • INVALID_ARGUMENT (参数错误)                               │   │
│   │  • NOT_FOUND (资源不存在)                                    │   │
│   │                                                              │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│   AI Decision:                                                       │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │                                                              │   │
│   │  retry_probability = model.predict(                          │   │
│   │      error_type,                                             │   │
│   │      node_health_score,                                      │   │
│   │      historical_retry_success_rate,                          │   │
│   │      current_system_load                                     │   │
│   │  )                                                           │   │
│   │                                                              │   │
│   │  if (retry_probability > 0.5) {                              │   │
│   │      retry_with_different_node();                            │   │
│   │  } else {                                                    │   │
│   │      fail_fast();                                            │   │
│   │  }                                                           │   │
│   │                                                              │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📦 模块规划

### 核心模块

```
ai-rpc-framework/
├── ai-rpc-api/                    # 核心API定义
├── ai-rpc-core/                   # 核心实现
│   ├── transport/                 # 网络传输
│   ├── codec/                     # 编解码
│   ├── loadbalance/               # 负载均衡
│   ├── circuitbreaker/            # 熔断器
│   ├── retry/                     # 重试策略
│   └── timeout/                   # 超时策略
│
├── ai-rpc-brain/                  # AI大脑 (新增)
│   ├── collector/                 # 指标收集
│   ├── features/                  # 特征工程
│   ├── models/                    # ML模型
│   │   ├── exponential/           # 指数衰减
│   │   ├── lightgbm/              # LightGBM
│   │   └── neural/                # 神经网络
│   └── prediction/                # 预测服务
│
├── ai-rpc-registry/               # 注册中心
│   ├── nacos/
│   ├── zookeeper/
│   └── consul/
│
├── ai-rpc-spring-boot-starter/    # Spring集成 (新增)
│
├── ai-rpc-tracing/                # 分布式追踪 (新增)
│
├── ai-forecasting-service/        # Python AI服务
│
└── examples/
    ├── quickstart/
    ├── spring-boot-example/
    └── ai-features-demo/
```

---

## 🚀 Roadmap

### Phase 1: 基础完善 (v1.0 - 2周)

- [x] 核心RPC功能
- [x] AI负载均衡基础版
- [x] Nacos注册中心
- [x] README + LICENSE
- [ ] 修复硬编码BUG
- [ ] 完善.gitignore
- [ ] 添加单元测试
- [ ] GitHub Actions CI

### Phase 2: Spring集成 (v1.1 - 2周)

- [ ] Spring Boot Starter
- [ ] @AiRpcService 注解
- [ ] @AiRpcReference 注解
- [ ] YAML配置支持
- [ ] 自动装配

### Phase 3: AI增强 (v1.2 - 3周)

- [ ] 多维指标收集
- [ ] AI-CircuitBreaker
- [ ] AI-Timeout
- [ ] AI-Retry
- [ ] 模型可插拔SPI

### Phase 4: 生态完善 (v1.3 - 2周)

- [ ] Zookeeper注册中心
- [ ] Consul注册中心
- [ ] OpenTelemetry追踪
- [ ] Prometheus指标暴露
- [ ] Grafana Dashboard模板

### Phase 5: 文档与社区 (持续)

- [ ] 完整用户文档
- [ ] 开发者文档
- [ ] Benchmark报告
- [ ] 博客文章
- [ ] 示例项目

---

## 📊 核心卖点

### 1. AI-Native
不是事后添加AI，而是从设计之初就将AI作为核心

### 2. 零配置自适应
无需调参，系统自动优化

### 3. 可解释的AI
每个决策都有clear的reason，方便调试

### 4. 渐进式采用
可以只用传统功能，也可以开启AI功能

### 5. 生产验证
来自腾讯工程师的实践经验

---

## 🎯 目标用户

### 主要用户
- 微服务架构的中大型企业
- 对系统稳定性要求高的金融/电商
- 想要减少运维配置工作的团队

### 使用场景
- 服务间RPC调用
- 流量治理
- 故障自愈
- 性能优化

---

**文档版本:** v0.1
**创建日期:** 2026-01-21
**作者:** wangyouwei
